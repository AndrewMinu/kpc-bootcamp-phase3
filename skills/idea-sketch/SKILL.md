---
name: idea-sketch
description: 로컬 싱크 관광 부트캠프 5단계 — 검증된 문제를 풀 솔루션 아이디어를 팀 회의로 발산·수렴하도록 돕는다. 참가자가 "아이디어 짜줘", "솔루션 뭐로 할까", "뭘 만들까", "기능 정하자", "MVP 뭐로 할까", "아이디어 스케치", "회의 붙여줘" 라고 할 때 사용한다. context.md의 주제·지역·데이터검증을 바탕으로 사용자·페인포인트·차별점·시나리오·성공기준·MVP를 팀이 채우게 돕고 기록한다.
---

# 5단계 · 솔루션 아이디어 스케치 (idea-sketch)

## 목적
검증된 문제를 풀 **솔루션 아이디어**를 팀 회의로 좁힌다. 꼭 '앱'일 필요는 없지만, 부트캠프 산출은 **2박3일 안에 단일 HTML로 데모 가능한** 형태로 수렴한다. 스킬은 답을 주지 않고 **팀 회의를 이끈다**(퍼실리테이터).

## 시작 전
- `context.md`의 `주제`·`지역`·`데이터 검증 요약`을 읽는다.
- 카드 경로면 카드(`bootcamp-shared/cards/`, 없으면 `ft-skills/pipeline/cards/`)의 **6(예상 아웃풋)·7(추천 MVP)** 를 *씨앗*으로만 참고(정답 아님, 변형 권장).
- 스코프 감각: 핵심 기능 1~2개, 단일 HTML 데모.

## 회의 컨텍스트 — 팀이 함께 채운다 (핵심)
아이디어를 바로 고르지 말고, 아래 **7가지를 팀 대화로 채운다.** 각 항목을 개방형 질문으로 던지고, 4명이 각자 답한 뒤 모은다. 이게 채워질수록 회의가 풍부해지고 발표도 탄탄해진다.

1. **사용자 정의** — 정확히 누구? (여행자면 어떤 여행자: 뚜벅이·가족·외국인·반려인…) *"우리 사용자 한 명을 구체적으로 그려볼까요? 나이·상황·왜 여기 왔나?"*
2. **페인포인트 순간** — 그 사람이 **언제·어디서** 막히나? *"여행 흐름(오기 전→도착→낮→저녁→다음날) 중 정확히 어디서 불편해요?"*
3. **지금의 대안** — 지금은 그걸 어떻게 (불편하게) 해결하나? *"지금은 사람들이 어떻게 해요? 왜 그게 별로예요?"*
4. **우리의 한 방(차별점)** — 그 순간에 우리가 뭘 **다르게** 하나? *"똑같은 걸 남들도 하는데, 우리만의 다른 점 한 줄은?"*
5. **데이터·기능 연결** — 그게 4단계에서 본 **데이터/기능으로 실제 되나?** 안 되면 축소. *"이 기능, 우리가 받은 데이터로 굴러가요?"*
6. **사용 시나리오 1개** — 사용자가 **처음부터 끝까지** 쓰는 이야기 한 편. *"화면을 켜서→뭘 하고→뭘 얻고 끝나는지 한 장면으로."*
7. **성공하면 뭐가 달라지나** — 지역/사용자에게 어떤 변화? (발표 임팩트) *"이게 잘 되면 제천에 뭐가 남아요?"*

> 위 7개를 다 완벽히 채울 필요는 없지만, **1·2·4·6은 반드시** 팀 언어로 남긴다.

## 발산 → 수렴 (퍼실리테이션)
- **발산**: *"각자 이 문제를 풀 아이디어 하나씩. 누구를 위한 건지도."* 4명이 낸 뒤 화이트보드처럼 모아 보여준다. 안 나오면 접근 예시(코스 추천/지도/큐레이션/챗봇/알림)를 **보조로**.
- **수렴**: 팀 합의로 1개. 갈리면 *"발표 때 뭘 보여주면 제일 세 보일까요? 데이터로 뒷받침되는 건 뭐죠?"* 로 팀이 판단. **핵심 기능 1~2개**로.
- **MVP 한 줄**: `입력 → (AI)처리 → 출력 → 발표화면`. 예: "관심사·시간 입력 → 관광지+숙박 매칭 → 1박 코스 생성 → 발표용 지도".

## 피칭 4요소 미리 심기
아이디어에 **문제 근거(데이터)·사용자 가치·AI 활용·데모 가능성**이 들어가는지 팀과 점검(발표 심사 대비).

## 심화 (시간 있으면 — 2박3일 깊이)
- **여러 라운드 발산**: 각자 3개씩 빠르게(크레이지8 식) → 붙여놓고 투표 → 상위 2개를 짧게 비교 후 택1.
- `field-research`(인터뷰·여정 매핑)로 얻은 **실제 발견을 반영**해 아이디어를 벼린다.
- **경쟁/유사사례**와 대보며 차별점을 한 문장으로 날카롭게.
- 컨셉 2개를 각각 한 컷 스케치로 그려 팀이 비교(막상 그려보면 약점이 보인다).

## context.md 갱신
- `아이디어`에 위 회의 컨텍스트(사용자·페인포인트·대안·차별점·시나리오·성공기준)와 핵심 기능·MVP 흐름을 팀 언어로 기록.
- `단계: 5`, 로그 추가.

## 끝맺음
- 요약: "솔루션 = [한 줄], MVP = [흐름]".
- **다음: `prototype-build`** 로 작동 프로토타입을 만들고, `pitch-deck-build`로 발표 덱을 만듭니다.

## 톤
- 기능을 늘리려는 팀은 "발표에서 보여줄 1개"로 되돌린다.
- 정답 제시가 아니라 **팀이 스스로 채우게** 이끈다.
